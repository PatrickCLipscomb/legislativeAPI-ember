bills in the week around a selected day

<div class="row">
  <div class="col-xs-6">
    <h3>All Committees</h3>
    <ul>
      {{#each model.committees as |committee|}}
        <li>{{committee.name}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="col">
    <h3>Committees related to keyword</h3>
    <ul>
      {{#each model.keywordCommittees as |committee|}}
        <li>{{committee.name}}</li>
      {{/each}}
    </ul>
  </div>
</div>
